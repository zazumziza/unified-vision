export type TrackCard = {
  id: string
  title: string
  artist: string
  scene: string
  cover: string
  duration: string
  verified?: boolean
}

export type EventCard = {
  id: string
  title: string
  venue: string
  date: string
  city: string
  image: string
}
